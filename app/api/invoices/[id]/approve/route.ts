import { prisma } from "@/lib/prisma";
import { assertSameOrigin, jsonError, requireUser } from "@/lib/auth";

export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; assertSameOrigin(request); const user = await requireUser(["ADMIN", "FINANCE"]); const body = await request.json().catch(() => ({})); const approvalStatus = ["APPROVED", "REJECTED"].includes(body.approvalStatus) ? body.approvalStatus : null; if (!approvalStatus) return Response.json({ error: "Status approval tidak valid" }, { status: 400 }); const invoice = await prisma.invoice.findUnique({ where: { id } }); if (!invoice) return Response.json({ error: "Invoice tidak ditemukan" }, { status: 404 }); if (invoice.status !== "DRAFT") return Response.json({ error: "Hanya draft invoice yang dapat di-approve" }, { status: 409 }); const updated = await prisma.$transaction(async (tx) => { const result = await tx.invoice.update({ where: { id }, data: { approvalStatus, approvedById: user.id, approvedAt: new Date() } }); await tx.auditLog.create({ data: { actorId: user.id, action: approvalStatus === "APPROVED" ? "INVOICE_APPROVED" : "INVOICE_REJECTED", objectType: "Invoice", objectId: id, reason: typeof body.reason === "string" ? body.reason.trim().slice(0, 1000) : undefined } }); return result; }); return Response.json({ invoice: updated }); } catch (error) { return jsonError(error); } }
